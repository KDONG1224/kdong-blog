import { MainContainer } from 'containers';
import { MainLayout } from 'layouts';

const Homepage = () => {
  return (
    <MainLayout>
      <MainContainer />
    </MainLayout>
  );
};

export default Homepage;
