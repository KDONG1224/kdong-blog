// layouts
import { MainLayout } from 'layouts';

// containers
import { MainContainer } from 'containers';

const Homepage = () => {
  return (
    <MainLayout>
      <MainContainer />
    </MainLayout>
  );
};

export default Homepage;
