// base
import React, { useEffect, useRef } from 'react';

// style
import { StyledPortfolio } from './style';

export const Portfolio = () => {
  return (
    <StyledPortfolio>
      <div>Ss</div>
    </StyledPortfolio>
  );
};
