import styled from '@emotion/styled';

export const StyledImageBankList = styled.div`
  .ibl-wrapper {
    position: relative;

    p {
      margin: 0 !important;
    }

    &-right {
      position: absolute;
      right: 0;
      top: 0;
    }
  }
`;
