import styled from '@emotion/styled';
import { Swiper } from 'swiper/react';

interface StyledBasicSwiperProps {
  ismobile: boolean;
}

export const StyledBasicSwiper = styled(Swiper)<StyledBasicSwiperProps>``;
